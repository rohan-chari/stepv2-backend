# Character wardrobe release candidate — September 9, 2026

The backend retains the frozen-client carry-over equip contract and adds independent saved character outfits. No production database, runtime, catalog item, grant, price, or flag was changed by implementation.

## Concrete releases and deployment boundary

Release A is `b30db8e` (includes `577b439` and `7b33d48`): additive migration `20260909190000_character_wardrobes`, shared user-first writer lock, slot diffs, dual writes/revisions, null unequip support, bounded compatibility cleanup and resumable projection-authoritative repair. **It has no wardrobe v1 routes.** Release B is `30ee4d4` and adds the four routes after that compatible-writer boundary. Roll B back to A, never to a pre-A writer while v1 routes remain reachable. Keep all additive schema and inactive outfits on rollback.

The approved production sequence, requiring separate in-the-moment production authorization, is:

1. Apply the additive migration and deploy A using the existing drain/reload procedure. Preserve exactly two HTTP workers and leave staging stopped. Verify the running source of every cosmetics writer process and drain old requests; process health alone does not prove this boundary.
2. Run `node scripts/repair-character-wardrobes.js` for a bounded dry-run page. If its response has `nextCheckpoint`, continue with `--after=<checkpoint>`. Only after explicit production authorization run matching `--apply` pages. Each page handles at most 200 users, locks each user, and copies active projection to materialized saved state; inactive wardrobes and ownership stay untouched. Re-run dry-run pages until anomalies are zero.
3. Re-run `node scripts/audit-character-wardrobes.js` after migration to check the new saved-state references. Run `node scripts/apply-character-wardrobe-fits.js` to validate the explicit content mapping, then separately authorized `--apply` to install its 56 approved pairs atomically. No appearance or ownership changes occur.
4. Deploy B using the same capacity/drain procedure. Check the four new read contracts, existing old-client catalog/equipment behavior and A/B writer version evidence. New app rollout comes after B readiness.

Routine B rollback restores A. Newer apps receive the unsupported-wardrobe fallback; old slot-based equips still synchronize saved state. A-only, mixed A+B, and rollback-to-A HTTP behavior were exercised using two actual checkouts and one local dedicated test DB by `scripts/verify-character-wardrobe-release-protocol.js <release-A-checkout>`. All three checks passed; final appearance revision was 4 after A/B/null writes.

## Contract and content

The four request/response contracts match the approved frontend spec, with the coordinated additive `canPreview` property. ShopItem IDs are opaque text; real legacy IDs such as `shop-baseball-cap` remain supported. `canPreview` permits approved unowned sale-item try-on; `canSelect` still requires ownership and Save cannot grant purchases. See `docs/evidence/character-wardrobe-contract-fixtures.json` for exact public responses captured from real HTTP.

GETs use read-only REPEATABLE READ snapshots, keyset limits up to 48, and no wardrobe materialization. The default character appears only on the first collection page. Hidden equipment IDs are redacted and make replacement unavailable. Inactive edits do not modify active/public projection or invalidate public appearance. Changed active saves/activation use the same best-effort presentation and auth cache invalidation as legacy writers. No-op saves do not advance revisions or touch equipment timestamps. Real warm Redis/public friend HTTP tests verify inactive saves leave cached appearance unchanged and activation/active saves update the next response; command-selective EVAL/DEL failures retain stale Redis data while the existing bypass reads fresh PostgreSQL state.

`data/character-wardrobe-fits.json` explicitly records 67 accessory identities: 19 released accessories approved for default/corgi, 18 for turtle (shoes excluded), total 56 pairs. Crown art rendered a fallback and remains preservation-only; test-only items have no newly approved fit. Real avatar/art contact sheets and the full production SELECT-only report are retained in the frontend's `docs/evidence/full-screen-shop/`. Existing actual saved/current combinations remain grandfathered without granting new fit rights.

The read-only production audit at 2026-09-09T21:37:05Z checked 69 catalog rows and 227 purchase replay records: 22 Preserve released, 13 Preserve owned-or-referenced, 34 Needs investigation, **zero safe retirement candidates**. Saved-item tables were absent because production is pre-A; rerun after migration. Birthday Hat is released, reward eligible and protected (12 owners, 3 equipped, 8 daily reward references in that snapshot). The permanent retirement manifest is intentionally empty until historical release proof is reviewed. Unknown historical release status cannot be treated as unreleased.

Audit and retirement commands preserve all art, history, ownership and dynamic reward-pool members. The separate retirement apply command validates expected row fingerprints and references under locks, aborts changed candidates, and invalidates shared catalog/asset caches after commit. The manifest prevents future admin/mirror/clone/sync reactivation. The ordinary Prisma seed does not seed cosmetics.

## Verification and measured work

Local targets were explicitly verified as `steps-tracker-integration_test` and `wardrobe-compat-review_test` on localhost/::1 before writes. The additive migration succeeded on both an existing test schema and a fresh disposable schema. Initial real HTTP tests failed on missing routes/schema; relevant final suites pass 28 tests (49/49 when combined with old shop/accessory/admin regressions; no skips) covering public contracts, ownership/fit/hidden/earned behavior, atomic conflicts, null/carry-over writes, no-op identity/timestamps, deterministic concurrent snapshot/save behavior, Home appearance, reward pool/economic invariants, deletion cascades, repair, fit application and dry-run audits. The review-found character-removal repair regression first failed on A with P2002, then passed with the default wardrobe included in its bounded read.

`docs/evidence/character-wardrobe-query-counts.json` records full HTTP query events, including auth, membership/ad policy and cache database work. Local collection and wardrobe reads used five SELECTs each and zero writes. Inactive save and activation each used three domain SELECTs plus three explicit locks. First inactive save creates its own wardrobe plus an initial active checkpoint; this one-time materialization is included. A no-op produces zero INSERT/UPDATE/DELETE. There are no new jobs, step-sync hooks or economic writes. BEGIN/COMMIT emission depends on Prisma's adapter event stream; counts are observed events, not a claim about hidden network protocol frames.

`docs/evidence/character-wardrobe-query-plans.json` retains EXPLAIN ANALYZE for actual public HTTP SQL shapes on small local fixtures. PostgreSQL preferred sequential scans at those sizes; this does not prove population-scale performance. The schema includes identity/fit/ownership lookup indexes. No speculative additional ShopItem index was added: the observed 69-row production catalog and tiny local plans did not justify its write cost. No production CPU savings are claimed.

Full unit suite: 3,370 tests, 3,364 passed and 6 failed initially. One new source-inventory failure was mechanically updated for the move to the shared writer and an extra guard now pins all four caller paths (4/4 targeted cache inventory passes). The other five failures reproduce unchanged in the isolated pre-A source `3292872`: Inbox wake fixture, three global entitlement fixture cases, and race mutation AST inventory. Existing assertions were not weakened. The broader integration suite result and final B hash will be appended after completion; no green full-suite claim is made here.

Full integration completed: 3,155 tests, 3,103 passed, 51 failed, one existing skip (949 seconds). No wardrobe suite failed. The 27 failing existing suites are being compared against isolated pre-A source; do not claim a green full suite or classify unreproduced failures as baseline. The final isolated targeted run passed 49/49, including 28 new wardrobe/Redis/operations tests. The subsequent audit hardening counts both IDs and SKUs for SSV grants and replay references; its pending-grant test first failed, then all four operations tests passed.
