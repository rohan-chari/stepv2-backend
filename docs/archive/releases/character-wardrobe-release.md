# Character wardrobe release candidate — September 9, 2026

The backend retains the frozen-client carry-over equip contract and adds independent saved character outfits. No production database, runtime, catalog item, grant, price, or flag was changed by implementation.

## Concrete releases and deployment boundary

The production-based candidate starts at deployed `1bcf874f5cd8f2a84e7dbb980aada9c3feca9325`, preserving all seven immediate-join commits absent from the original development checkout. Release A is **`00baf02b29e30ccb7c964c493d2d4d76405c2b4a`** (branch `shop-wardrobe-release-a`): additive migration `20260909190000_character_wardrobes`, compatible shared writers/locks/revisions, null unequip, repair, and the account-deletion and presentation fallback corrections described below. **A has no wardrobe v1 routes.** Release B's route implementation is **`65fbea7`**, followed by audit hardening `538dc3c` and reviewed fixture/evidence commits on branch `shop-wardrobe-release-candidate`. The final candidate commit is the commit containing this report; exact source/test hashes and results are in `docs/evidence/character-wardrobe-production-candidate-checks.json`.

Roll B back to A, never to a pre-A writer while wardrobe routes remain reachable. Keep additive schema and inactive outfits on rollback. The original development hashes and original broad-suite counts retained in older evidence are historical, **not this production-based candidate's validation**. Both rebases onto corrected A preserved the entire candidate tree byte-for-byte.

The approved production sequence, requiring separate in-the-moment production authorization, is:

1. Apply the additive migration and deploy A using the existing drain/reload procedure. Preserve exactly two HTTP workers and leave staging stopped. Verify the running source of every cosmetics writer process and drain old requests; process health alone does not prove this boundary.
2. Run `node scripts/repair-character-wardrobes.js` for a bounded dry-run page. If its response has `nextCheckpoint`, continue with `--after=<checkpoint>`. Only after explicit production authorization run matching `--apply` pages. Each page handles at most 200 users, locks each user, and copies active projection to materialized saved state; inactive wardrobes and ownership stay untouched. Re-run dry-run pages until anomalies are zero.
3. Re-run `node scripts/audit-character-wardrobes.js` after migration to check the new saved-state references. Run `node scripts/apply-character-wardrobe-fits.js` to validate the explicit content mapping, then separately authorized `--apply` to install its 56 approved pairs atomically. No appearance or ownership changes occur.
4. Deploy B using the same capacity/drain procedure. Check the four new read contracts, existing old-client catalog/equipment behavior and A/B writer version evidence. New app rollout comes after B readiness.

Routine B rollback restores A. Newer apps receive the unsupported-wardrobe fallback; old slot-based equips still synchronize saved state. A-only, mixed A+B, and rollback-to-A HTTP behavior were exercised using two actual checkouts and one local dedicated test DB by `scripts/verify-character-wardrobe-release-protocol.js <release-A-checkout>`. All three checks passed; final appearance revision was 4 after A/B/null writes.

## Contract and content

The four request/response contracts match the approved frontend spec, with the coordinated additive `canPreview` property. ShopItem IDs are opaque text; real legacy IDs such as `shop-baseball-cap` remain supported. `canPreview` permits approved unowned sale-item try-on; `canSelect` still requires ownership and Save cannot grant purchases. See `docs/evidence/character-wardrobe-contract-fixtures.json` for exact public responses captured from real HTTP.

GETs use read-only REPEATABLE READ snapshots, keyset limits up to 48, and no wardrobe materialization. The default character appears only on the first collection page. Hidden equipment IDs are redacted and make replacement unavailable. Inactive edits do not modify active/public projection or invalidate public appearance. Changed active saves/activation use the same best-effort presentation and auth cache invalidation as legacy writers. No-op saves do not advance revisions or touch equipment timestamps. Real warm Redis/public friend HTTP tests verify inactive saves leave cached appearance unchanged and activation/active saves update the next response; command-selective EVAL/DEL failures retain stale Redis data while the existing bypass reads fresh PostgreSQL state.

`data/character-wardrobe-fits.json` explicitly records 67 accessory identities: 19 released accessories approved for default/corgi, 18 for turtle (shoes excluded), total 56 pairs. Crown art rendered a fallback and remains preservation-only; test-only items have no newly approved fit. Real avatar/art contact sheets and the full production SELECT-only report are retained in the frontend's `docs/evidence/full-screen-shop/`. Existing actual saved/current combinations remain grandfathered without granting new fit rights.

The read-only production audit at 2026-09-09T21:50:28Z checked 69 catalog rows and 227 purchase replay records: 22 Preserve released, 13 Preserve owned-or-referenced, 34 Needs investigation, **zero safe retirement candidates**. Saved-item tables were absent because production is pre-A; rerun after migration. Birthday Hat is released, reward eligible and protected (12 owners, 3 equipped, 8 daily reward references in that snapshot). The permanent retirement manifest is intentionally empty until historical release proof is reviewed. Unknown historical release status cannot be treated as unreleased.

Audit and retirement commands preserve all art, history, ownership and dynamic reward-pool members. The separate retirement apply command validates expected row fingerprints and references under locks, aborts changed candidates, and invalidates shared catalog/asset caches after commit. The manifest prevents future admin/mirror/clone/sync reactivation. The ordinary Prisma seed does not seed cosmetics.

## Candidate verification and limits

**Source review: SHIP. Unconditional done/deployment readiness: withheld.** All shop-specific checks and the final unit suite are green, but existing integration assertions remain red. The repository rule forbids calling the work done with red tests; reproducing a failure on deployed source does not waive that rule. No deployment, production data write, staging start, flag, new SKU, art, or economy change was performed.

Local databases were verified by `current_database()` and server address before writes: `wardrobe-release-candidate_test`, `steps-tracker-integration_test`, and `wardrobe-production-baseline_test` on localhost/::1. PostgreSQL18 validation used an isolated cluster at127.0.0.1:55549, database `wardrobe_pg18_test`, version18.4; only that temporary cluster was stopped afterward. All candidate migrations were applied locally, including the additive wardrobe migration. No migration was applied in production.

Final validation:

- Full unit suite: **3,371/3,371**, no failures/skips, after the presentation fallback correction.
- Full production-based candidate integration run: **3,225 tests, 3,142 passed, 82 failed, one existing skip**, 1,121 seconds. No wardrobe suite failed. This completed run predates the final narrow fixture/fallback corrections; do not invent an updated full-suite total by subtraction.
- Exact deployed-source baseline, rerunning only the31 failed suites: **477 tests,390 passed,87 failed**. Initial comparison found74 matching named cases (69 normalized signatures and five differing signatures), eight unmatched candidate cases, and13 baseline-only failures. Later narrow checks resolved these differences explicitly in the linked inventory.
- Final completed-summary and new wardrobe public Redis tests: **8/8**. Real Redis warm-cache reads, failed invalidation, active save/activation, and inactive-save presentation isolation remain covered.
- Final strict fixture cases: **9/9**, including Leech/frozen/mine parity, overdue-box recovery, repeated source corrections, preparation/handoff/watchdog recovery, and explicitly unset Redis. Scenario claims use the real `claimNext` and `processOne` path with exact race ownership; no assertion or debounce/fence requirement was relaxed.
- PostgreSQL18 historical empty-page regression: **1/1**,749 buffers against the unchanged8,000-budget assertion; comparison statement35,099 buffers. The original local16 run failed the explicit version prerequisite, not a demonstrated optimizer regression.
- Final A-only/mixed A+B/rollback HTTP protocol: all three **true**, appearance revision4, after A gained both compatibility corrections.
- Final all-wardrobe/account-deletion suite: **29/29**, no failures/skips. Both formerly failing signup-recovery cases additionally pass **2/2 on candidate and baseline** after deterministic catalog isolation. No tests remain running at handoff.

Two production-compatible corrections were authorized after tests exposed them. Real signup now creates seeded assignment rows; account deletion hit an FK error while deleting participants. A and B now remove only that user's bucket assignments and window memberships inside the existing fenced/locked deletion transaction before participant removal. The real signup→DELETE regression verifies peer preservation, historical anonymization, complete live removal, and trigger-induced transactional rollback. Exact deployed source reproduces the500; corrected A and B pass.

The shared presentation reader previously propagated a thrown Redis collaborator error instead of using its PostgreSQL loader. The real Redis wrapper ordinarily catches transport failures, but the existing HTTP outage regression exposed the missing outer guard. Both deployed source and candidate reproduce500 when generation-guard cache coverage is explicit. The correction uses the existing error fallback and metrics. New wardrobe cache tests now restore prior persistent settings, and the existing completed-summary test explicitly enables/restores generation-guard coverage. Thus test order no longer determines whether the public outage assertion exercises this path.

## Remaining work outside the shop

`docs/evidence/character-wardrobe-production-baseline-comparison.json` retains every named original failure, actual/expected signature, baseline comparison, latest narrow result, and exact residual suite list. Categories are evidence-based triage, not a claim that every failure is harmless:

- **Resolved seeded recovery fixture contamination:** both `LEGACY` and `BUCKET` late-signup-recovery cases initially counted59/8 active memberships, expected2. The baseline retained six custom seed rows from earlier suites; grouping the failed subject showed exactly one membership for each retained seed, including the canonical two. The suite now activates only its canonical daily/weekly catalog while snapshotting/restoring prior seed settings. Both original assertions pass **2/2 on candidate and exact deployed baseline**, with all custom rows retained. No production recovery defect is established by these failures.
- **Standings/index contract:** `race-bootstrap-persisted-standings.test.js` has the exact finish/placement/steps/join/user ordering case and three bounded indexed first-page cases for50/500/5000 participants. SQL now ends ties with participant ID while the existing index/test expects user ID. The16-vs15 assertion is EXPLAIN Actual Rows, not SQL query count. A separate index/order design decision and migration review are needed.
- **Source-generation performance fixture:** `race-queue-v2-closure-scaling.test.js`,477-player maximum case. The fixture inserts new samples without advancing cached immutable scoring generations:475 cached users retain one box and two uncached users receive four, producing483 vs1908. Correcting generation setup requires focused prior-feature verification; no payout numbers were changed. Other performance suites remain listed individually rather than presumed equivalent.
- **Deployed policy versus older expectations:** retired DECOY visibility in `powerups5-wave.test.js`, canonical tournament `powerupsEnabled` in `tournaments.test.js`, and pre-migration team LEAVE/FORFEIT in `race-leave-capability.test.js`. Additional powerup duration/expiry cases need policy/fixture review. Restoring old behavior merely to satisfy tests would exceed shop scope.
- **Older signup and queue/event assumptions:** admin/membership counts, onboarding, enrollment/discovery/referral fixtures and remaining queue/post-task/notification/event/cache cases reproduce deployed behavior or have demonstrated unscoped job assumptions. Corrected cases are marked green individually. Remaining failures require scoped investigation rather than blanket assertion changes. The last complete two-queue-suite diagnostic had50/70 pass before the final scoped mine/handoff/watchdog corrections; no new broad total is claimed.

The read-only production source recheck after the long validation window still returned `1bcf874f5cd8f2a84e7dbb980aada9c3feca9325`; filtered process metadata showed exactly two online production workers and staging stopped. Existing authenticated metrics evidence records managed PostgreSQL18.6 (`docs/db-cpu-diagnosis-2026-09-08.md`). Production state was not changed.

## Measured wardrobe work

`docs/evidence/character-wardrobe-query-counts.json` records complete HTTP query events, including authentication and existing policy/cache reads. Collection and wardrobe GETs used five SELECTs each and zero writes. Inactive save and activation used three domain SELECTs plus three locks. First materialization includes an initial active checkpoint; a no-op emits zero INSERT/UPDATE/DELETE. No new jobs, step-sync hooks, coin mutations, or reward weights were added.

`docs/evidence/character-wardrobe-query-plans.json` retains EXPLAIN ANALYZE for the real public query shapes. Small fixture plans and the69-row catalog do not establish population-scale performance or production CPU savings. Existing wardrobe/fit/ownership identity indexes cover the new access paths; no speculative ShopItem index was added. Contract fixtures, query counts, fitting/retirement/repair evidence and historical red-before-green tests remain retained alongside the new candidate evidence.
